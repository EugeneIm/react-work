import React from 'react'
import Nav from '../components/Nav';

function Header() {
	return (
		<div className="total-header">
			<header>
				<h1>
				<a href="/">Boring News</a>
				</h1>
			</header>
		</div>
	)
}

//Header, this should contain :
   //Header element
   //Main Website title that's linkable to the main page (just dummy links) in an H1 tag
   //Website navigation

export default Header